<UserControl x:Class="Superheater.Pages.EditorPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:usc="clr-namespace:Superheater.UserControls"
             xmlns:viewmodels="clr-namespace:Superheater.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type viewmodels:EditorViewModel}}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="Pink">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <usc:SearchBar Grid.Row="0" Margin="5"/>

        <usc:EditorLists Grid.Row="1" Margin="5"/>

        <usc:ProgressBar Grid.Row="3" Margin="5"/>

        <usc:EditorButtons Grid.Row="2"/>

    </Grid>
</UserControl>
