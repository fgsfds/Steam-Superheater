<UserControl x:Class="SteamFD.Pages.MainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:usc="clr-namespace:SteamFD.UserControls"
             xmlns:viewmodels="clr-namespace:SteamFD.ViewModels"
             mc:Ignorable="d"
             x:Name="MainWindow"
             d:DataContext="{d:DesignInstance {x:Type viewmodels:MainViewModel}}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <usc:SearchBar Grid.Row="0" Margin="5"/>

        <usc:MainListsControl Grid.Row="1" Margin="5"/>

        <usc:ProgressBar Grid.Row="3" Margin="5"/>

        <usc:MainButtons Grid.Row="2" Margin="5"/>

        <usc:GameInfoBar Grid.Row="4" Margin="5"/>

    </Grid>
</UserControl>
